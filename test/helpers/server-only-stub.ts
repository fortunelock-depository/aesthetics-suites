// Empty stand-in for the `server-only` package, which throws when imported
// outside a React Server environment. Vitest aliases it here so the server
// modules under test can be imported directly in node.
export {};
